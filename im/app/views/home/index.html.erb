<% content_for :head do %>
    <%# stimulusコントローラーのconnect()が２回動かないようにするために必要 %>
    <meta name="turbo-cache-control" content="no-cache">
<% end %>

<h1>Home#index</h1>
<p>
  <div data-controller="hello"
    data-all="<%= {
      testValue: 'テスト' + Time.now.to_s,
      }.to_json %>"
  >
    元の文字
  </div>
</p>
