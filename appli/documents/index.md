# アプリ

アプリ用APIの導線のみ。

ネイティブで動かす方法の詳細は、`capacitor-test`参照。

## API

Webとは別のアプリ用のAPIを用意してます。

ベースURI /api/appli

## その他

WEBアプリケーションのアプリ化は、メンテナンスコストが、一気に高くなるので、やめておいたほうが無難。

これは、実装例というだけで、非推奨。

pwaにしておくほうが無難。
